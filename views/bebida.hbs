<div class="container">
    <div class="row">
        <div class="col-sm-12">
            <article>
                <h1>{{nombre}}</h1>
                <img src="{{img_url}}" alt="{{nombre}}" class="img-fluid">
                <h3>{{descripcion}} </h3>
                {{#if hasIngredients}}
                {{ingredientes}}
                {{/if}}
            </article>
        </div>
    </div>
</div>